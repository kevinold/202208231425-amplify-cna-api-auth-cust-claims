type Todo
  @model
  @auth(
    rules: [
      { allow: owner }
      {
        allow: groups
        groupsField: "group"
        groupClaim: "user_groups"
        operations: [create, read, update, delete]
      }
    ]
  ) {
  id: ID!
  name: String!
  description: String
  group: String
}
